// 시작 방법:
// 1) P13 answer.ino를 복사합니다.
// 2) 아래 TODO를 채우며 점검용 버전으로 정리합니다.
//
// 실패 시 트리아지(고장 분류) 순서:
// 1) 상태 전이 로그 먼저 확인
// 2) LED 표시 확인
// 3) SD 로그 확인
// 앞 단계가 실패하면 다음 단계로 넘어가지 말고 먼저 수정하세요.

// TODO 1: 상태 전이 reason 로그를 모두 점검
// STEP 1) 시리얼 로그에서 상태 전이가 올바른 순서인지 확인하세요.
// STEP 2) 각 전이에 reason 문자열이 빠지지 않았는지 확인하세요.
// FAIL 시 우선 조치) updateState()와 setState() 로그 출력을 먼저 수정하세요.

// TODO 2: LED 패턴을 상태별로 정리
// STEP 3) 상태별 LED 동작표(점멸 주기/고정 여부)를 작성하세요.
// STEP 4) 실제 동작이 표와 일치하는지 상태별로 관찰하세요.
// FAIL 시 우선 조치) updateIndicators() 분기와 점멸 타이머 변수를 점검하세요.

// TODO 3: SD 로그 헤더/주기 점검
// STEP 5) CSV 헤더가 1회만 작성됐는지 확인하세요.
// STEP 6) 로그가 목표 주기(100ms 근사)로 누적되는지 확인하세요.
// FAIL 시 우선 조치) setupLogging()/logData()의 주기 조건과 파일 열기 실패 처리부터 수정하세요.

// TODO 4: 체크리스트 1회 수행
// STEP 7) 전체 흐름 1회 완주, reason 로그, LED/시리얼 일치, CSV 정상 여부를 한 번에 점검하세요.

void setup() {
  // 이 파일은 "코드 작성"보다 "최종 점검"이 목적입니다.
  // 실구현은 P13의 완성 코드를 복사해 유지하세요.
}

void loop() {
}
